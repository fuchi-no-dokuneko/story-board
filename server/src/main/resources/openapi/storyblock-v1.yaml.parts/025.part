          type: string
        kind:
          type: string
        media_type:
          type: string
        codec:
          type: string
        content_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        size_bytes:
          type: integer
          minimum: 0
          maximum: 16777216
        created_at:
          type: string
          format: date-time
        content_base64:
          type: string
          contentEncoding: base64
      additionalProperties: false
    RenderRequest:
      type: object
      required: [revision_id]
      properties:
        revision_id:
          type: string
        from_block_id:
          type: [string, "null"]
        to_block_id:
          type: [string, "null"]
      additionalProperties: false
    PdfRenderRequest:
      type: object
      required: [revision_id]
      properties:
        revision_id:
          type: string
          pattern: '^rev_[A-Za-z0-9]{5}$'
      additionalProperties: false
    BlockImage:
      type: object
      required: [artifact_id, content_hash, media_type, width_px, height_px, alt_text]
      properties:
        artifact_id:
          type: string
          pattern: '^art_[0-9a-f-]{36}$'
        content_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        media_type:
          type: string
          enum: [image/png, image/jpeg]
        width_px:
          type: integer
          minimum: 1
          maximum: 8192
        height_px:
          type: integer
          minimum: 1
          maximum: 8192
        alt_text:
          type: string
          minLength: 1
          maxLength: 2000
          x-storyblock-max-graphemes: 500
      additionalProperties: false
    ImageUploadResponse:
      type: object
      required: [schema_version, artifact_id, artifact_uri, content_hash, media_type, width_px, height_px, idempotent_replay]
      properties:
        schema_version:
          const: image-upload-1.0.0
        artifact_id:
          type: string
          pattern: '^art_[0-9a-f-]{36}$'
        artifact_uri:
          type: string
          format: uri-reference
        content_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        media_type:
          type: string
          enum: [image/png, image/jpeg]
        width_px:
          type: integer
          minimum: 1
          maximum: 8192
        height_px:
          type: integer
          minimum: 1
          maximum: 8192
        idempotent_replay:
          type: boolean
      additionalProperties: false
    RenderPacket:
      type: object
      required: [novel_id, revision_id, revision_hash, renderer_version, range, rendered_text, blocks, resolved_meta, offset_map, scene_boundaries]
      properties:
        novel_id:
