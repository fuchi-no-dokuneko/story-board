          type: string
          pattern: ^rev_[A-Za-z0-9]{5}$
        in: query
      responses:
        '200':
          description: All chapters at a fixed novel revision.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChapterList'
        default:
          $ref: '#/components/responses/Problem'
  /novels/{novelId}/scenes:
    get:
      operationId: novels_scenes_list
      x-required-scopes: ["novel:read"]
      summary: Scenes selected by a half-open chapter index slice. No body text.
      parameters:
      - name: novelId
        required: true
        schema:
          type: string
          pattern: ^nov_[A-Za-z0-9]{5}$
        in: path
      - name: revision_id
        required: true
        schema:
          type: string
          pattern: ^rev_[A-Za-z0-9]{5}$
        in: query
      - name: chapter_start
        required: false
        schema: &id001
          type: integer
          minimum: 0
        in: query
      - name: chapter_end
        required: false
        schema: *id001
        in: query
      responses:
        '200':
          description: Scenes selected by a half-open chapter index slice. No body
            text.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SceneList'
        default:
          $ref: '#/components/responses/Problem'
  /novels/{novelId}/scenes/{sceneId}/blocks:
    get:
      operationId: novels_blocks_slice
      x-required-scopes: ["novel:read"]
      summary: Revision-bound scene block slice; [0,0) reads an empty scene.
      parameters:
      - name: novelId
        required: true
        schema:
          type: string
          pattern: ^nov_[A-Za-z0-9]{5}$
        in: path
      - name: sceneId
        required: true
        schema:
          type: string
          pattern: ^scn_[A-Za-z0-9]{5}$
        in: path
      - name: revision_id
        required: true
        schema:
          type: string
          pattern: ^rev_[A-Za-z0-9]{5}$
        in: query
      - name: start
        required: true
        schema: *id001
        in: query
      - name: end
        required: true
        schema: *id001
        in: query
      responses:
        '200':
          description: Revision-bound scene block slice; [0,0) reads an empty scene.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlockSlice'
        default:
          $ref: '#/components/responses/Problem'
  /style/:
    get:
      operationId: styles_list
      x-required-scopes: ["novel:read"]
      summary: Available server-side reference styles. Registry errors are explicit.
      parameters: []
      responses:
        '200':
