# Flyway Migrations

Versioned production migrations use the `V<version>__<description>.sql` naming
convention. Phase 0 reserves this location; the canonical schema starts with the
Phase 1 persistence work.
