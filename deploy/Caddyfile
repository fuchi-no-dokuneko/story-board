{$STORYBLOCK_SITE_ADDRESS:localhost} {
    encode zstd gzip

    @public path /v1 /v1/* /actuator/health
    handle @public {
        reverse_proxy api:8080
    }

    handle {
        respond "Not found" 404
    }
}
