# health.read

Read the complete machine-readable contract: [JSON](../endpoints.json).
完整機器可讀契約：[JSON](../endpoints.json)。
完整机器可读契约：[JSON](../endpoints.json)。

| Field / 欄位 / 字段 | Value / 值 / 值 |
| --- | --- |
| Method / 方法 / 方法 | GET |
| Path / 路徑 / 路径 | `/actuator/health` |
| Request / 請求 / 请求 | — |
| Response / 回應 / 响应 | 200: ActuatorHealthResponse |
| Scopes / 範圍 / 范围 | — |

The default local server trusts reachable clients; credentials are optional.
預設本機伺服器信任可連線裝置；密鑰為選填。
默认本机服务器信任可连接设备；密钥为选填。
