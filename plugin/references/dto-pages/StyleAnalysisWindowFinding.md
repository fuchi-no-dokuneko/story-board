# StyleAnalysisWindowFinding

Read the complete machine-readable contract: [JSON](../dtos/StyleAnalysisWindowFinding.schema.json).
完整機器可讀契約：[JSON](../dtos/StyleAnalysisWindowFinding.schema.json)。
完整机器可读契约：[JSON](../dtos/StyleAnalysisWindowFinding.schema.json)。

The schema includes field constraints, examples, and source provenance. Validate with the CLI before sending a request.
綱要包含欄位限制、範例及來源。送出請求前先使用 CLI 驗證。
纲要包含字段限制、示例及来源。发送请求前先使用 CLI 验证。
