# StoryBlock Engine Specification

The authoritative version 0.9 specification is maintained at
`../../story-board.md` in the parent workspace. This repository implements that
document and records contract decisions in `docs/adr`.

The parent document is deliberately not copied here so local-only development
does not create two independently edited specifications.
