# Observability

EN: Health and aggregate metrics retain their endpoints. Local reachable clients receive operator access by default. Operational logs stay under server/data/logs; SQLite stores redacted audit events. Health thresholds and worker-age settings remain configurable at startup.

繁體中文：健康與彙總指標保留既有端點。本機可連線客戶端預設取得操作權限。執行日誌留在 server/data/logs；SQLite 儲存遮蔽後稽核事件。健康門檻與工作佇列年齡仍可在啟動時設定。

简体中文：健康与汇总指标保留既有端点。本机可连接客户端默认获得操作权限。运行日志留在 server/data/logs；SQLite 保存脱敏审计事件。健康阈值与工作队列年龄仍可在启动时设置。

[Detailed notes / 詳細筆記 / 详细笔记](observability.notes.txt)
