# HTTP contract

EN: Every existing endpoint, request/response schema, status contract, and database migration is preserved. The default local deployment trusts reachable clients. Optional scoped access retains novel boundaries, including rewrite-proposal reads. OpenAPI and the code-derived author catalog remain available.

繁體中文：所有既有端點、請求與回應綱要、狀態契約及資料庫遷移均保留。預設本機部署信任可連線客戶端。選用範圍授權仍限制作品邊界，包含改寫提案讀取。OpenAPI 與程式導出的作者目錄保持可用。

简体中文：所有既有端点、请求与响应纲要、状态契约及数据库迁移均保留。默认本机部署信任可连接客户端。选用范围授权仍限制作品边界，包含改写提案读取。OpenAPI 与程序导出的作者目录保持可用。

[Detailed notes / 詳細筆記 / 详细笔记](v1-http-contract.notes.txt)
